% Copyright 2026 Caroline Blank <caro@c-space.org>
% SPDX-License-Identifier: CC-BY-NC-SA-4.0

```{metadata}
page-break-force: 2
page-break-avoid-inside: 3
```

# Révisions calcul littéral

`````{container} noprint
# Réduire une expression

``` {list-grid}
:style: grid-template-columns: 1fr 1fr;
-   Théorie
    ```{youtube} qEUb4IU-HiY
    ```
-   Exercices résolus
    ```{youtube} R6-mxyhgSmA
    ```
````
`````

`````{container} noprint
# Développer une expression

```{list-grid}
:style: grid-template-columns: 1fr 1fr;
-   Théorie
    ```{youtube} URNld8xsXgM
    ```
-   Exercices résolus
    ```{youtube} 7k5kFah3z7w
    ```
````
`````

`````{container} noprint
# Développer et réduire une expression

```{list-grid}
:style: grid-template-columns: 1fr 1fr;
-   Théorie
    ```{youtube} 4PTioyfnmqc
    ```
-   Exercices résolus
    ```{youtube} Dy0mS7kkAD8
    ```
````
`````

## Exercices

### Exercice {nump}`exercice`

Réduisez au maximum.

{.lower-alpha-paren .columns-2}
1. $a^2 \cdot 2ab =$
2. $3a \cdot (-2ab) =$
3. $4a^2 \cdot 5a \cdot 2b =$
4. $2a \cdot (-3a^2) \cdot (-2ab) =$
5. $5a^2 \cdot 3a^3 \cdot (-2a^2) =$
6. $7xy \cdot 3x^2 =$

```{block} solution
{.columns-4 .lower-alpha-paren}
1. $2a^3b$
2. $-6a^2b$
3. $40a^3b$
4. $12a^4b$
5. $-30a^7$
6. $21x^3y$
```

### Exercice {nump}`exercice`

Réduisez au maximum.

{.lower-alpha-paren .columns-2}
1. $a \cdot a \cdot a \cdot b \cdot b =$
2. $(a^2)^3 =$
3. $\dfrac{2y}{xy} =$
4. $4w + 5w - w =$
5. $(2a^2)^2 \cdot a^3 \cdot a^5 =$
6. $x \cdot x + 2x^2 =$

```{block} solution
{.columns-4 .lower-alpha-paren}
1. $a^3 \cdot b^2$
2. $a^6$
3. $\dfrac{2}{x}$
4. $8w$
5. $4a^{12}$
6. $3x^2$
```

### Exercice {nump}`exercice`

Développez.

{.lower-alpha-paren .columns-2}
1. $3a^2 \cdot (2ab + b^2) =$
2. $2a^3 \cdot (5a - 3b) =$
3. $4x^2 \cdot (5xy - x^2) =$
4. $(7ab - 3a^2) \cdot 3ab =$
5. $(4a^2b - 7ab^2) \cdot a^3 =$
6. $(3a - 2b) \cdot 7ab =$

```{block} solution
{.columns-3 .lower-alpha-paren}
1. $6a^3b + 3a^2b^2$
2. $10a^4 - 6a^3b$
3. $20x^3y - 4x^4$
4. $21a^2b^2 - 9a^3b$
5. $4a^5b - 7a^4b^2$
6. $21a^2b - 14ab^2$
```

### Exercice {nump}`exercice`

Développez et réduisez au maximum.

{.lower-alpha-paren .columns-2}
1. $3x(x + 5) - 2x =$
2. $4x(x - 3) + x^2 =$
3. $2x(y + 4) - 3xy =$
4. $5x(x + 2) - 10x^2 =$

```{block} solution
{.columns-4 .lower-alpha-paren}
1. $3x^2 + 13x$
2. $5x^2 - 12x$
3. $-xy + 8x$
4. $-5x^2 + 10x$
```

### Exercice {nump}`exercice`

Réduisez au maximum.

{.lower-alpha-paren}
1. $(2a + 5b) - (7a + 2b) =$
2. $(3x - 4y + z) + (2x - y + 2z) =$
3. $(4a^2 - 7a + 2) - (-2a^2 + 3a - 2) =$
4. $-(4x^2 - 2x + 4) + (-4x^2 - 7x + 1) =$
5. $(4ab^2 - 5a^2b) - (3ab^2 + 2a^2b) =$
6. $-(2a^3 - 3b^2) - (7a^3 + b^2) + (3a^3 - b^2) =$

```{block} solution
{.columns-3 .lower-alpha-paren}
1. $-5a + 3b$
2. $5x - 5y + 3z$
3. $6a^2 - 10a + 4$
4. $-8x^2 - 5x - 3$
5. $ab^2 - 7a^2b$
6. $-6a^3 + b^2$
```

### Exercice {nump}`exercice`

Développez et réduisez au maximum.

{.lower-alpha-paren}
1. $3(x + 4) - 2(5 - 3x) =$
2. $10 + 4y - 7(2y - 5) + 6 =$
3. $4x + 2x(3 - x) - 5\left(x^2 - 2x + 4\right) =$
4. $12 + 3x - 4x(2x - 3) + 7x(1 - x) =$
5. $9y + 6(2y - 5) + 7 + 4(2y - 8) =$

```{block} solution
{.columns-3 .lower-alpha-paren}
1. $9x + 2 =$
2. $-10y + 51 =$
3. $-7x^2 + 20x - 20 =$
4. $-15x^2 + 22x + 12 =$
5. $29y - 55 =$
```

### Exercice {nump}`exercice`

Développez et réduisez au maximum.

{.lower-alpha-paren}
1. $3b - \big(5a + 3ab - (4a - ab) - 9b\big) =$
2. $(3x^4 - 2x^2 + 2) \cdot (-2x^2 + 3) =$
3. $2x \cdot (3x - x^2 + 1) - 3 \cdot (x^2 - 2x) =$
4. $(a + 1) \cdot (2a - 3) - (a - 1) \cdot (a + 1) =$
5. $2x \cdot \big((x - 3) - (x - 2)\big) =$
6. $(3x - y) \cdot (3x - 2y) + (2x - y) \cdot x =$

```{block} solution
{.columns-3 .lower-alpha-paren}
1. $-a + 12b - 4ab$
2. $-6x^6 + 13x^4 - 10x^2 + 6$
3. $-2x^3 + 3x^2 + 8x$
4. $a^2-a-2$
5. $-2x$
6. $11x^2 - 10xy + 2y^2$
```

### Exercice {nump}`exercice`

Développez et réduisez au maximum.

{.lower-alpha-paren}
1. $\dfrac{4}{3}x^3y^3 \cdot (-3xy^3)^2 =$
2. $2a - \big(3b - (-5 + 3a) - 4\big) - 2a =$
3. $(2x^3 - 3y) \cdot (-3x^3 + y) =$
4. $x + y \cdot (-3x + 4y) =$
5. $(2x - 3y) \cdot (3x - y) - (2x - y) \cdot (5x + y) =$
6. $4x - y \cdot (x - 2) + 3x \cdot (5 + y) =$
7. $\left(3x^3 - 5x^2 + 10\right) + \left(2x^3 - 4x^2 + 8\right) =$
8. $\left(4x^2 + 3y\right) \cdot (-x + 5y) =$
9. $7x \cdot \left(3y \cdot x^2\right) =$
10. $2x \cdot \left(3y - x^2\right) =$
11. $6y^3 + \left(9y^3 - 4\right) - 12 =$
12. $3y^2 - 5y^2 \cdot (10y - 25) =$
13. $5x + 7x \cdot (4x - 2) =$
14. $-\left(4x^3 + 15xy\right) - 8x \cdot \left(-6x^2 - 9y\right) =$
15. $50 - (2x - 3)(3x - 20) =$
16. $(5x + 10) - (2x - 5)(3x + 3) =$

```{block} solution
{.columns-2 .lower-alpha-paren}
1. $12x^5y^9$
2. $3a - 3b - 1$
3. $-6x^6 + 11x^3y - 3y^2$
4. $x - 3xy + 4y^2$
5. $-4x^2 - 8xy + 4y^2$
6. $19x + 2xy + 2y$
7. $5x^3 - 9x^2 + 18$
8. $-4x^3 + 20x^2y - 3xy + 15y^2$
9. $21x^3y$
10. $-2x^3 + 6xy$
11. $15y^3 - 16$
12. $-50y^3 + 128y^2$
13. $28x^2 - 9x$
14. $44x^3 + 57xy$
15. $-6x^2 + 49x - 10$
16. $-6x^2 + 14x + 25$
```

## Solutions

```{blocks} solution
:class: allow-break-inside
```
